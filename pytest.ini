[pytest]
markers =
    agente: evaluación de capa B contra el proveedor real (gasta API del usuario)
; Por defecto solo corre la capa A (sin gastar API). La capa B se corre
; explícitamente con:  pytest -m agente -s
addopts = -m "not agente"
