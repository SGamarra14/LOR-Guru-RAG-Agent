# Secretos — NUNCA subir
.env
.env.*
!.env.example

# NOTA: data/ (cartas públicas) y chroma_db/ (índice de vectores, sin secretos)
# SÍ se versionan a propósito: el Dockerfile los copia y el host (Railway) los
# necesita al construir la imagen desde el repo. Se regeneran con
# `python -m lorguru.build_index` si cambian los datos o el modelo de embeddings.

# Python
__pycache__/
*.pyc
.pytest_cache/

# Node / Next.js
node_modules/
webapp/.next/
webapp/out/
webapp/next-env.d.ts

# Editor / SO
.vscode/
.idea/
Thumbs.db
.DS_Store
