# La imagen necesita lorguru/ + requirements-api.txt + los artefactos ya
# construidos (data/ y chroma_db/, que NO contienen secretos). Todo lo demás
# fuera. OJO: .env SIEMPRE excluido (el runtime recibe las claves por entorno).
.git
.env
.env.*
webapp/
tests/
scripts/
__pycache__/
*.pyc
.pytest_cache/
*.ipynb
*.md
